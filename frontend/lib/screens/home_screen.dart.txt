import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eurydice - Accueil'),
        centerTitle: true,
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(context, '/voices');
              },
              child: Text('🎤 Choisir la voix'),
            ),
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(context, '/ambiances');
              },
              child: Text('🎧 Choisir l\'ambiance musicale'),
            ),
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(context, '/instruments');
              },
              child: Text('🎼 Choisir les instruments'),
            ),
          ],
        ),
      ),
    );
  }
}
